<UserControl x:Class="MVVMApp.View.UserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMApp.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ListView ItemsSource="{Binding Userlist}" Margin="233,81,233,147">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding FirstName}" Width="100"/>
                    <GridViewColumn  Header="Age" DisplayMemberBinding="{Binding Email}" Width="100"/>
                    <GridViewColumn  Header="Address" DisplayMemberBinding="{Binding Address}" Width="100"/>

                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</UserControl>
